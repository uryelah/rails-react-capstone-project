import React from 'react';

const styles = {
  position: 'fixed',
  width: '100vw',
  heigth: '400px',
  backgroundColor: '#ff000088',
  fontWeight: 'bold',
  fontSize: '5rem',
  top: '25vh',
  zIndex: '900',
  textAlign: 'center',
};

const Wip = () => (
  <div style={styles}>
    Work in progress
  </div>
);

export default Wip;
